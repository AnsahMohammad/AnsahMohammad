# WiJungle Log-in Automation

download both network.sh and WiJungle.py

run `chmod +x network.sh`
for python, update your username and password on environment varaibles
run `pip install selenium`
then finally
`./network.sh`

